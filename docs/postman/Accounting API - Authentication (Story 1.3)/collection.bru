meta {
  name: Accounting API - Authentication (Story 1.3)
}

auth {
  mode: none
}

vars:pre-request {
  baseUrl: http://localhost:8080
  accessToken: 
  refreshToken: 
  testEmail: admin@example.com
  testPassword: password
  resetToken: 
  userId: 
}

docs {
  Postman collection for testing User Authentication & Security endpoints. Note: Registration endpoint has been removed. Users must be created by administrators.
}
